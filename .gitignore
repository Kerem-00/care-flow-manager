node_modules

# macOS
.DS_Store

# Build output (api-server rebuilds on pnpm dev; lib/*/dist kept for TS composite build)
artifacts/api-server/dist

# TypeScript incremental build cache
*.tsbuildinfo

# Environment files (never commit secrets)
.env
.env.local

# Replit
.cache/
.local/
